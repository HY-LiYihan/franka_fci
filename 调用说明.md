# FR3 控制服务调用说明

本文假设运行 FR3 控制服务的工控机 IP 是 `192.168.1.123`，API 端口是 `8000`。另一台电脑只需要和工控机处于同一网络，并能访问该地址。

## 1. 在工控机上允许局域网访问

服务默认只监听 `127.0.0.1`，其他电脑无法访问。请在工控机项目目录的 `.env` 中设置：

```dotenv
FRANKA_API_HOST=192.168.1.123
FRANKA_API_PORT=8000
```

然后启动服务：

```bash
uv run python -m franka_fci.main
```

也可以将 `FRANKA_API_HOST` 设置为 `0.0.0.0`，表示监听所有网卡；建议通过防火墙限制来源。当前服务不使用 Token，局域网访问权限由网络隔离和主机防火墙负责。不要把 `172.16.0.2` 填到 `FRANKA_API_HOST`，那个地址是机器人控制柜的 FCI 地址。

## 2. 从另一台电脑测试连接

在调用电脑上设置服务地址变量：

```bash
export FRANKA_URL=http://192.168.1.123:8000
```

查询健康状态：

```bash
curl "$FRANKA_URL/api/v1/health"
```

查询机器人和夹爪状态：

```bash
curl "$FRANKA_URL/api/v1/state"

curl "$FRANKA_URL/api/v1/gripper/state"
```

## 3. 发送关节位置命令

关节角单位是弧度，必须正好提供 7 个值。服务会立即返回命令 ID，不会等待运动完成：

```bash
curl -X POST "$FRANKA_URL/api/v1/motions/joint-position" \
  -H 'Content-Type: application/json' \
  -d '{
    "target_rad": [0.0, -0.4, 0.0, -2.0, 0.0, 1.6, 0.7],
    "duration_s": 3.0,
    "request_id": "client-001"
  }'
```

典型返回：

```json
{
  "command_id": "cmd_xxxxxxxxx",
  "status": "queued",
  "accepted_at": "2026-09-22T12:00:00Z"
}
```

使用返回的 `command_id` 查询结果：

```bash
curl "$FRANKA_URL/api/v1/commands/cmd_xxxxxxxxx"
```

状态通常依次为 `queued`、`running`、`succeeded`；也可能是 `stopped` 或 `failed`。

## 4. 发送夹爪末端位姿命令

位姿目标是基坐标系下的官方夹爪末端，位置单位是米，四元数顺序固定为 `xyzw`。标准 Franka Hand 工厂安装默认使用 `T_flange_gripper = translation [0, 0, 0.1034] m + quaternion [0, 0, 0, 1]`。如果现场安装了转接件或定义了不同工具 frame，请在 `.env` 中覆盖该值：

```bash
curl -X POST "$FRANKA_URL/api/v1/motions/cartesian-pose" \
  -H 'Content-Type: application/json' \
  -d '{
    "position_m": [0.45, 0.0, 0.35],
    "quaternion_xyzw": [0.0, 0.0, 0.0, 1.0],
    "frame_id": "base",
    "tool_frame": "gripper",
    "duration_s": 3.0,
    "request_id": "client-cartesian-001"
  }'
```

## 5. 调用夹爪

宽度单位是米，速度单位是米/秒，夹持力单位是牛顿：

```bash
# homing
curl -X POST "$FRANKA_URL/api/v1/gripper/homing"

# 移动到 4 cm
curl -X POST "$FRANKA_URL/api/v1/gripper/move" \
  -H 'Content-Type: application/json' \
  -d '{"width_m": 0.04, "speed_m_s": 0.05, "request_id": "gripper-move-001"}'

# 夹持目标宽度 2 cm、速度 2 cm/s、力 20 N
curl -X POST "$FRANKA_URL/api/v1/gripper/grasp" \
  -H 'Content-Type: application/json' \
  -d '{"width_m": 0.02, "speed_m_s": 0.02, "force_n": 20, "request_id": "gripper-grasp-001"}'
```

## 6. 受控停止

停止当前机械臂或夹爪命令：

```bash
curl -X POST "$FRANKA_URL/api/v1/stop"
```

这不是物理急停。发生危险时应立即使用现场急停和示教器安全机制。

## 7. 常见问题

- `422`：JSON 字段、单位、数组长度或数值不符合接口要求。
- `409`：已有运动命令，或同一个 `request_id` 被用于不同命令。
- `503`：机器人未连接、FCI 断开、状态过期、机器人存在错误。
- 连接超时：在调用电脑执行 `ping 192.168.1.123`，在工控机确认 API 监听地址和防火墙端口 `8000`；同时确认调用电脑和工控机的网段、路由及有线网络配置正确。

首次现场测试应先只调用 `/health` 和 `/state`，确认状态正常后再发送低速、短距离运动命令。
