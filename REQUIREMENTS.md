# franka_fci 需求说明

**版本**：v0.1 草案
**状态**：初始化阶段，待实现和现场验证
**目标**：通过 `pylibfranka` 控制一台 Franka FR3 机器人，并向同一局域网内的授权主机提供可审计、可停止的机械臂和末端夹爪指令接口。FR3 控制柜在机器人侧工控机上的 FCI 地址为 `172.16.0.2`。

## 1. 范围

### 1.1 本阶段包含

- 在机器人侧工控机建立和维护一个指向 `172.16.0.2` 的 `pylibfranka`/FCI 连接。
- 读取并缓存机器人健康状态、关节状态、末端状态和错误信息。
- 读取并缓存 flange 位姿和通过固定安装变换计算出的官方夹爪末端位姿。
- 提供 HTTP/JSON 局域网接口；访问控制由网络隔离和主机防火墙负责，不使用 Token。
- 接收有限时长的 7 关节位置和笛卡尔位姿目标，并由服务端生成平滑轨迹。
- 通过 Franka Gripper 接口执行 homing、移动开口宽度和受限力控夹持。
- 将 API 提供的夹爪末端位姿目标转换为 `pylibfranka` 所需的 flange 目标。
- 支持查询命令状态和请求停止当前运动。
- 对输入、状态、权限、并发命令和网络断开进行 fail-closed 处理。
- 为没有真实机器人时的 API、校验和命令状态机提供 fake adapter 测试路径。

### 1.2 明确不包含

- 物理急停、人员保护或任何安全认证功能；这些由机器人控制柜、示教器和现场安全系统负责。
- 将 HTTP 请求、WebSocket 消息或普通 Python 网络回调直接作为 1 kHz 伺服 setpoint。
- v0.1 的任意关节力矩、关节速度、碰撞阈值或底层控制器参数远程透传。
- 自动清除碰撞、急停、FCI 断开等安全错误。
- 多机器人编排、互联网暴露、用户管理系统或云端存储。

## 2. 使用者与部署边界

| 角色 | 责任 |
| --- | --- |
| 现场操作者 | 配置 FCI、碰撞行为、急停和工作空间；在运动前确认人员与障碍物安全 |
| 机器人主机 | 运行服务、持有唯一 `pylibfranka.Robot`/夹爪连接、执行实时控制循环 |
| 局域网客户端 | 通过 API 发送已授权的有限时长目标，并处理命令状态和错误 |
| 服务维护者 | 锁定依赖版本、维护网络访问策略、审阅日志和兼容性记录 |

系统假定“一台服务进程控制一台机器人”。多机器人或多控制进程需要单独的资源隔离和安全设计。

## 3. 运行约束

1. 机器人主机使用经过验证的 Ubuntu、Python 和有线网络配置；客户端操作系统不影响 FCI 实时链路。
2. 目标机器人型号为 FR3，FCI 控制柜地址为 `172.16.0.2`；服务不得把该地址用作 API 监听地址。
3. `pylibfranka` 版本、底层 libfranka、控制柜固件和 FCI 版本必须作为一个兼容组合验收。
4. 机器人 IP 必须显式配置为 `172.16.0.2`；服务启动时检查网络可达性和 FCI 状态。
5. API 默认只监听 `127.0.0.1`。监听局域网地址必须显式配置，并由主机防火墙限制来源。

## 4. 功能需求

### FR-01 配置与生命周期

- 支持配置机器人地址、API 监听地址/端口、状态过期阈值、单条运动最大时长和允许来源。
- 启动时加载配置并拒绝缺失或格式错误的关键项。
- 服务可以在机器人暂时不可达时启动为 `degraded`，但不得接受运动；健康接口仍需说明具体原因。
- 关闭时先拒绝新命令，再请求当前控制线程安全停止，最后释放机器人连接。

### FR-02 机器人适配层

- 所有 `pylibfranka` 调用集中在适配层，不让 HTTP 层依赖具体绑定细节。
- 一个进程只能有一个活动的机器人连接、一个夹爪连接和一个控制循环；禁止并发调用互斥的控制入口。
- 连接建立后应用已审阅的默认碰撞行为和控制参数；不得从不可信客户端直接覆盖。
- 适配层暴露连接状态、机器人模式、错误列表和最新状态快照。
- 适配层必须配置固定的 `T_flange_gripper` 安装变换，方向定义为 FR3 flange 到官方夹爪末端 frame。
- 标准 Franka Hand 工厂安装可使用默认 `translation [0,0,0.1034] m`、`quaternion [0,0,0,1]`；若存在转接件或不同工具 frame，必须改为现场机械尺寸或标定记录。

### FR-03 实时控制隔离

- 控制回调必须使用预先计算或无阻塞的数据；不得执行 HTTP、DNS、磁盘、动态网络日志或不可控内存分配。
- API 请求通过线程安全队列/状态机提交目标，控制线程负责消费目标并驱动 `pylibfranka`。
- 轨迹必须有明确的开始、结束和最大时长；不能因为客户端不再响应而无限运动。
- 控制循环异常、状态过期、命令超时或机器人错误时，系统必须停止接受新目标并进入可观测的故障状态。

### FR-04 运动命令

v0.1 支持以下三类有限时长目标：

1. **关节位置**：7 个 `target_rad`，单位 rad，带 `duration_s`。
2. **夹爪末端笛卡尔位姿**：`position_m[3]`（m）和 `quaternion_xyzw[4]` 表示 `T_base_gripper`，带 `duration_s` 和明确参考坐标系，默认使用机器人基坐标系；目标工具坐标固定为官方 `gripper` frame。
3. **末端夹爪**：homing；移动到 `width_m`（m）；以 `width_m`、`speed_m_s`（m/s）和 `force_n`（N）执行 grasp。

所有请求必须：

- 校验 JSON schema、数组长度、数值有限性、单位和姿态归一化。
- 对笛卡尔目标按 `T_base_flange = T_base_gripper · inverse(T_flange_gripper)` 做变换，再提交给 `pylibfranka`；不得把夹爪末端目标直接当作 flange 目标。
- 校验服务端配置的工作空间、速度/加速度/时长上限，并让机器人/`pylibfranka` 做最终约束检查。
- 拒绝错误状态、状态缓存过期、已有活动命令或未经授权的请求。
- 返回不可猜测的 `command_id`，并支持使用客户端提供的 `request_id` 做幂等去重；重复请求不得产生第二次运动。

v0.1 不接收原始力矩、速度或每周期 setpoint，也不接受任意夹爪电机电流。连续控制需要另行设计 watchdog、传输协议和安全审查。

### FR-05 命令状态机

每条命令至少经历以下状态之一：

```text
accepted → queued → running → succeeded
                    ├──────→ stopped
                    └──────→ failed
accepted ────────────────→ rejected
```

命令记录至少包含 `command_id`、`request_id`（如果有）、来源地址、目标类型、创建/开始/结束时间、最终状态和机器可读错误码。服务一次只允许一个 `running` 命令；并发运动请求返回冲突，不静默排队。

### FR-06 状态与健康

- `GET /api/v1/health` 返回服务版本、机器人连接状态、FCI 状态、机器人模式、当前命令 ID、错误摘要和状态新鲜度。
- `GET /api/v1/state` 返回最新缓存，至少包括主机时间戳、机器人时间戳、7 关节位置/速度、flange 位姿、由安装变换计算的 gripper 位姿和错误信息。
- `GET /api/v1/gripper/state` 返回夹爪宽度、是否抓取到物体、运动状态、homing 状态和错误信息。
- 状态超过 `state_stale_after_ms` 未更新时标记为 `stale`，并禁止新的运动请求。
- 状态接口是只读的；不得因为查询请求触发连接重试或运动。

### FR-07 停止与故障处理

- `POST /api/v1/stop` 是高优先级的受控停止请求，可以停止当前命令并记录原因。
- API 停止不等于物理急停；文档和响应中都必须明确这一点。
- 对碰撞、急停、FCI 断开和控制柜错误只报告状态并进入故障；不得自动清除、反复重连或重放运动。
- 客户端超时、连接断开或进程收到终止信号时，不能留下无界的运动命令。

### FR-08 网络接口与访问边界

- API 使用 HTTP/JSON，前缀为 `/api/v1`，请求和响应使用 UTF-8。
- 所有接口不要求 Token；局域网暴露必须依赖网络分段和主机防火墙限制来源。
- 默认关闭 CORS；如确有浏览器客户端，只允许配置的来源。
- 建议由主机防火墙做来源 IP 限制；不把“局域网”视为可信边界。

### FR-09 可观测性

- 输出结构化日志，关联 `command_id`、`request_id`、来源地址和机器人状态。
- 记录连接建立/断开、命令接收/拒绝/完成/停止/失败和安全错误。
- 目标数据只在满足审计需要时保留。
- 为状态过期、控制回调异常和命令超时提供可计数的指标或等价日志事件。

## 5. API 契约（v1 草案）

### 5.1 端点

| 方法 | 路径 | 成功响应 | 说明 |
| --- | --- | --- | --- |
| `GET` | `/api/v1/health` | `200` | 服务和机器人健康摘要 |
| `GET` | `/api/v1/state` | `200` | 最新状态快照 |
| `POST` | `/api/v1/motions/joint-position` | `202` | 接收关节位置目标 |
| `POST` | `/api/v1/motions/cartesian-pose` | `202` | 接收笛卡尔位姿目标 |
| `POST` | `/api/v1/gripper/homing` | `202` | 执行 Franka Gripper homing |
| `POST` | `/api/v1/gripper/move` | `202` | 移动到指定开口宽度 |
| `POST` | `/api/v1/gripper/grasp` | `202` | 执行受限速度/力的夹持 |
| `GET` | `/api/v1/gripper/state` | `200` | 查询夹爪状态 |
| `GET` | `/api/v1/commands/{command_id}` | `200` | 查询命令状态 |
| `POST` | `/api/v1/stop` | `200` | 请求受控停止 |

### 5.2 典型请求

```json
{
  "target_rad": [0.0, -0.4, 0.0, -2.0, 0.0, 1.6, 0.7],
  "duration_s": 3.0,
  "request_id": "client-20260921-0001"
}
```

笛卡尔位姿请求使用：

```json
{
  "position_m": [0.45, 0.0, 0.35],
  "quaternion_xyzw": [0.0, 0.0, 0.0, 1.0],
  "frame_id": "base",
  "tool_frame": "gripper",
  "duration_s": 3.0,
  "request_id": "client-20260921-0002"
}
```

夹爪移动请求使用：

```json
{
  "width_m": 0.04,
  "speed_m_s": 0.05,
  "request_id": "client-20260922-0003"
}
```

夹持请求使用：

```json
{
  "width_m": 0.02,
  "speed_m_s": 0.03,
  "force_n": 20.0,
  "request_id": "client-20260922-0004"
}
```

成功接收的命令响应示例：

```json
{
  "command_id": "cmd_01J...",
  "status": "accepted",
  "accepted_at": "2026-09-21T15:00:00Z"
}
```

至少定义以下错误语义：

| HTTP 状态 | 场景 |
| --- | --- |
| `400` / `422` | JSON、单位、维度或数值校验失败 |
| `403` | 来源不允许（由部署层防火墙负责） |
| `409` | 已有活动命令或请求 ID 冲突 |
| `503` | 机器人未连接、状态过期或处于故障 |

## 6. 非功能需求

### NFR-Safety（P0）

- 未满足连接、健康、权限和限制条件时默认拒绝运动。
- 任何远程接口都不能替代现场急停。
- 所有远程运动必须有限时、可追踪、可停止。
- 不通过自动重试绕过安全错误。

### NFR-Realtime（P0）

- 网络延迟不能进入 `pylibfranka` 实时回调的关键路径。
- 在实验室环境中验证控制循环频率、回调抖动和异常停止行为；具体阈值以现场控制方案批准值为准。

### NFR-Reliability（P1）

- 服务重启、网络断开和机器人断开都应留下明确状态，不得悄悄重放命令。
- API 接受响应应在正常负载下快速返回 `202`，不等待整段运动完成。
- 状态缓存必须带新鲜度信息，避免客户端误用旧数据。

### NFR-Security（P0/P1）

- 默认本机监听；局域网暴露必须显式打开并配置防火墙规则。
- 主机防火墙、网络分段和 TLS 属于部署验收项。

### NFR-Maintainability（P1）

- `pylibfranka` 适配层、命令状态机、API schema 和配置彼此解耦。
- API 通过 `/api/v1` 版本化；破坏性变更不得静默覆盖旧契约。
- 真实机器人测试与无机器人单元测试分离，不能让 CI 依赖现场设备。

## 7. 验收标准

### 脱机验收

- schema 能拒绝错误维度、NaN/Inf、超时长和非法四元数的请求。
- 使用已知 `T_flange_gripper` 做正向和逆向变换测试，确认 API 的 gripper 目标不会被误当成 flange 目标。
- fake adapter 下能覆盖命令状态机、幂等、冲突、停止、异常和重启恢复。
- API 线程和控制线程之间没有直接网络调用；日志和测试能证明这一点。
- 未连接或状态过期时，所有运动请求稳定返回 `503`，不会调用运动适配器。

### 现场验收（需具备安全审批）

1. 只读启动、健康和状态查询。
2. 无效输入、重复请求和并发请求验证。
3. 低速、受限空间内执行一条 7 关节位置运动。
4. 低速执行一条末端位姿运动，确认基坐标系和单位。
5. 执行夹爪 homing、move、grasp，并验证宽度/速度/力约束。
6. 验证 API 停止、进程终止、网络断开和 FCI 断开路径。
7. 验证碰撞/急停错误不会被自动清除或重放。
8. 记录经过验证的 FR3、固件、FCI、`pylibfranka`、Python 和操作系统版本。

## 8. 待决事项

- 现场 FR3 控制柜固件和 FCI/libfranka 兼容版本。
- 是否需要 TLS、客户端证书或仅使用网络隔离 + 防火墙。
- 服务部署形式（systemd、容器或现场已有编排系统）。
- 轨迹限制、碰撞行为和允许的工作空间参数，由谁审批和维护。
- 是否在 v1 之后增加 WebSocket 状态流、连续遥操作或 ROS 2 适配。
